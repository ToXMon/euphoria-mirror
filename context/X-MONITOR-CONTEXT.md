# X Monitor Context

Current x-monitor monitors `lowkeyfr`, parses bullish crypto signals, resolves tokens/liquidity, and has trading disabled/dry-run enabled in config. For this project, x-monitor should feed signals into Condor routines, not execute trades directly.
