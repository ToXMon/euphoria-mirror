# Kronos Context

Kronos is a financial candlestick/K-line foundation model. Use it for forecast signals only. Required OHLC columns: open, high, low, close. Optional: volume, amount. Treat output as probabilistic and log model/version/context.
